<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>กรอกข้อมูลการสั่งซื้อ</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    body {
      font-family: 'Sarabun', sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px 0;
    }
    
    .checkout-container {
      max-width: 800px;
      margin: 0 auto;
    }
    
    .card {
      border: none;
      border-radius: 20px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.1);
      backdrop-filter: blur(10px);
      background: rgba(255,255,255,0.95);
    }
    
    .card-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 20px 20px 0 0 !important;
      border: none;
      color: white;
      padding: 2rem;
    }
    
    .product-image {
      width: 100%;
      height: 200px;
      object-fit: cover;
      border-radius: 15px;
      border: 3px solid #f8f9fa;
    }
    
    .quantity-control {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 15px;
    }
    
    .quantity-btn {
      width: 50px;
      height: 50px;
      border: none;
      border-radius: 50%;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      font-size: 1.2rem;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    
    .quantity-btn:hover {
      transform: scale(1.1);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }
    
    .quantity-display {
      font-size: 2rem;
      font-weight: 600;
      color: #667eea;
      min-width: 60px;
      text-align: center;
    }
    
    .form-control {
      border-radius: 15px;
      border: 2px solid #e9ecef;
      padding: 15px 20px;
      font-size: 1rem;
      transition: all 0.3s ease;
    }
    
    .form-control:focus {
      border-color: #667eea;
      box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
    }
    
    .form-label {
      font-weight: 600;
      color: #495057;
      margin-bottom: 10px;
    }
    
    .price-summary {
      background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
      border-radius: 20px;
      padding: 2rem;
      margin: 2rem 0;
    }
    
    .price-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 15px;
      font-size: 1.1rem;
    }
    
    .price-total {
      border-top: 2px solid #dee2e6;
      padding-top: 15px;
      font-size: 1.3rem;
      font-weight: 700;
      color: #667eea;
    }
    
    .btn-submit {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border: none;
      border-radius: 15px;
      padding: 18px 40px;
      font-size: 1.2rem;
      font-weight: 600;
      color: white;
      width: 100%;
      transition: all 0.3s ease;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    
    .btn-submit:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
      background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
    }
    
    .product-info {
      background: white;
      border-radius: 15px;
      padding: 1.5rem;
      margin-bottom: 2rem;
      border: 1px solid #e9ecef;
    }
    
    .badge-status {
      background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
      color: white;
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 0.9rem;
    }
    
    .expiry-info {
      background: linear-gradient(135deg, #ffc107 0%, #fd7e14 100%);
      color: white;
      padding: 15px;
      border-radius: 15px;
      text-align: center;
      margin-top: 20px;
    }
    
    .floating-label {
      position: relative;
    }
    
    .floating-label input,
    .floating-label textarea {
      padding-top: 25px;
    }
    
    .floating-label label {
      position: absolute;
      top: 50%;
      left: 20px;
      transform: translateY(-50%);
      transition: all 0.3s ease;
      pointer-events: none;
      color: #6c757d;
    }
    
    .floating-label input:focus ~ label,
    .floating-label input:not(:placeholder-shown) ~ label,
    .floating-label textarea:focus ~ label,
    .floating-label textarea:not(:placeholder-shown) ~ label {
      top: 8px;
      font-size: 0.8rem;
      color: #667eea;
      transform: translateY(0);
    }
    
    @media (max-width: 768px) {
      .checkout-container {
        padding: 0 15px;
      }
      
      .card-header {
        padding: 1.5rem;
      }
      
      .quantity-control {
        gap: 10px;
      }
      
      .quantity-btn {
        width: 40px;
        height: 40px;
      }
      
      .quantity-display {
        font-size: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <div class="checkout-container">
    <div class="card">
      <!-- Header -->
      <div class="card-header text-center">
        <h2 class="mb-0">
          <i class="fas fa-shopping-cart me-3"></i>
          ข้อมูลการสั่งซื้อ
        </h2>
        <p class="mb-0 mt-2 opacity-75">
          คำสั่งซื้อ #<%= @order.order_number %>
        </p>
      </div>
      
      <div class="card-body p-4">
        <!-- Product Information -->
        <div class="product-info">
          <div class="row align-items-center">
            <div class="col-md-4 text-center mb-3 mb-md-0">
              <img src="https://via.placeholder.com/300x200/667eea/ffffff?text=<%= @order.product.productName %>" 
                   class="product-image" 
                   alt="<%= @order.product.productName %>">
            </div>
            <div class="col-md-8">
              <h4 class="text-primary mb-3">
                <i class="fas fa-box me-2"></i>
                <%= @order.product.productName %>
              </h4>
              <p class="text-muted mb-2">
                <i class="fas fa-align-left me-2"></i>
                <%= @order.product.productDetail %>
              </p>
              <p class="mb-2">
                <i class="fas fa-barcode me-2"></i>
                <strong>รหัสสินค้า:</strong> <%= @order.product.productCode %>
              </p>
              <p class="mb-0">
                <i class="fas fa-tag me-2"></i>
                <strong>ราคาต่อชิ้น:</strong> 
                <span class="text-success fw-bold fs-5">
                  <%= number_to_currency(@order.unit_price, unit: "฿", precision: 0) %>
                </span>
              </p>
            </div>
          </div>
        </div>
        
        <!-- Order Form -->
        <%= form_with model: @order, url: checkout_path(@order.checkout_token), 
                     method: :patch, local: true, class: "needs-validation", 
                     novalidate: true, id: "checkout-form" do |form| %>
          
          <!-- Quantity Selection -->
          <div class="mb-4">
            <label class="form-label">
              <i class="fas fa-sort-numeric-up me-2"></i>
              จำนวนสินค้า
            </label>
            <div class="quantity-control">
              <button type="button" class="quantity-btn" onclick="decreaseQuantity()">
                <i class="fas fa-minus"></i>
              </button>
              
              <%= form.hidden_field :quantity, id: "quantity_hidden" %>
              <div class="quantity-display" id="quantity_display"><%= @order.quantity %></div>
              
              <button type="button" class="quantity-btn" onclick="increaseQuantity()">
                <i class="fas fa-plus"></i>
              </button>
            </div>
            <small class="text-muted d-block text-center mt-2">
              สามารถสั่งซื้อได้ 1-10 ชิ้น
            </small>
          </div>
          
          <!-- Customer Information -->
          <h5 class="mb-4 text-primary">
            <i class="fas fa-user me-2"></i>
            ข้อมูลผู้สั่งซื้อ
          </h5>
          
          <div class="row">
            <div class="col-md-6 mb-3">
              <div class="floating-label">
                <%= form.text_field :customer_name, 
                                   class: "form-control", 
                                   placeholder: " ",
                                   required: true %>
                <label>
                  <i class="fas fa-user me-1"></i>
                  ชื่อ-นามสกุล
                </label>
              </div>
            </div>
            
            <div class="col-md-6 mb-3">
              <div class="floating-label">
                <%= form.text_field :customer_phone, 
                                   class: "form-control", 
                                   placeholder: " ",
                                   pattern: "[0-9]{10}",
                                   required: true %>
                <label>
                  <i class="fas fa-phone me-1"></i>
                  เบอร์โทรศัพท์
                </label>
              </div>
            </div>
          </div>
          
          <div class="mb-3">
            <div class="floating-label">
              <%= form.email_field :customer_email, 
                                  class: "form-control", 
                                  placeholder: " " %>
              <label>
                <i class="fas fa-envelope me-1"></i>
                อีเมล (ไม่บังคับ)
              </label>
            </div>
          </div>
          
          <div class="mb-4">
            <div class="floating-label">
              <%= form.text_area :customer_address, 
                                rows: 3, 
                                class: "form-control", 
                                placeholder: " ",
                                required: true %>
              <label>
                <i class="fas fa-map-marker-alt me-1"></i>
                ที่อยู่จัดส่ง
              </label>
            </div>
          </div>
          
          <div class="mb-4">
            <div class="floating-label">
              <%= form.text_area :notes, 
                                rows: 2, 
                                class: "form-control", 
                                placeholder: " " %>
              <label>
                <i class="fas fa-sticky-note me-1"></i>
                หมายเหตุ (ไม่บังคับ)
              </label>
            </div>
          </div>
          
          <!-- Price Summary -->
          <div class="price-summary">
            <h5 class="text-center mb-4">
              <i class="fas fa-calculator me-2"></i>
              สรุปการสั่งซื้อ
            </h5>
            
            <div class="price-row">
              <span>
                <i class="fas fa-tag me-2"></i>
                ราคาต่อชิ้น:
              </span>
              <span id="unit_price"><%= number_to_currency(@order.unit_price, unit: "฿", precision: 0) %></span>
            </div>
            
            <div class="price-row">
              <span>
                <i class="fas fa-times me-2"></i>
                จำนวน:
              </span>
              <span id="quantity_text"><%= @order.quantity %> ชิ้น</span>
            </div>
            
            <div class="price-row price-total">
              <span>
                <i class="fas fa-money-bill-wave me-2"></i>
                ราคารวมทั้งสิ้น:
              </span>
              <span id="total_amount"><%= number_to_currency(@order.total_amount, unit: "฿", precision: 0) %></span>
            </div>
          </div>
          
          <!-- Submit Button -->
          <button type="submit" class="btn-submit">
            <i class="fas fa-check-circle me-2"></i>
            ยืนยันข้อมูลและดำเนินการต่อ
          </button>
          
        <% end %>
        
        <!-- Expiry Information -->
        <div class="expiry-info">
          <i class="fas fa-clock me-2"></i>
          <strong>ลิงค์นี้จะหมดอายุใน:</strong>
          <span id="countdown"></span>
        </div>
        
        <!-- Debug Info (Development Only) -->
        <% if Rails.env.development? %>
          <div class="mt-4 p-3 bg-light border rounded">
            <h6><i class="fas fa-bug me-2"></i>Debug Info:</h6>
            <small>
              <strong>Order ID:</strong> <%= @order.id %><br>
              <strong>Status:</strong> <%= @order.status %><br>
              <strong>Token:</strong> <%= @order.checkout_token %><br>
              <strong>Facebook User:</strong> <%= @order.facebook_user_name %>
            </small>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  
  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
  <script>
    let currentQuantity = parseInt('<%= @order.quantity %>');
    const unitPrice = parseFloat('<%= @order.unit_price %>');
    const maxQuantity = 10;
    const minQuantity = 1;
    
    function updateQuantity(newQuantity) {
      if (newQuantity >= minQuantity && newQuantity <= maxQuantity) {
        currentQuantity = newQuantity;
        
        // อัพเดท UI
        document.getElementById('quantity_display').textContent = currentQuantity;
        document.getElementById('quantity_hidden').value = currentQuantity;
        document.getElementById('quantity_text').textContent = currentQuantity + ' ชิ้น';
        
        // คำนวณราคารวม
        const total = unitPrice * currentQuantity;
        document.getElementById('total_amount').textContent = '฿' + total.toLocaleString();
        
        // เพิ่มเอฟเฟกต์
        const display = document.getElementById('quantity_display');
        display.style.transform = 'scale(1.2)';
        display.style.color = '#28a745';
        
        setTimeout(() => {
          display.style.transform = 'scale(1)';
          display.style.color = '#667eea';
        }, 200);
      }
    }
    
    function increaseQuantity() {
      if (currentQuantity < maxQuantity) {
        updateQuantity(currentQuantity + 1);
      } else {
        // เอฟเฟกต์เมื่อถึงขีดจำกัด
        const display = document.getElementById('quantity_display');
        display.style.color = '#dc3545';
        setTimeout(() => {
          display.style.color = '#667eea';
        }, 300);
      }
    }
    
    function decreaseQuantity() {
      if (currentQuantity > minQuantity) {
        updateQuantity(currentQuantity - 1);
      } else {
        // เอฟเฟกต์เมื่อถึงขีดจำกัด
        const display = document.getElementById('quantity_display');
        display.style.color = '#dc3545';
        setTimeout(() => {
          display.style.color = '#667eea';
        }, 300);
      }
    }
    
    // Countdown Timer
    function updateCountdown() {
      const expiryTime = new Date('<%= @order.checkout_token_expires_at.iso8601 %>');
      const now = new Date();
      const timeDiff = expiryTime - now;
      
      if (timeDiff > 0) {
        const hours = Math.floor(timeDiff / (1000 * 60 * 60));
        const minutes = Math.floor((timeDiff % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((timeDiff % (1000 * 60)) / 1000);
        
        document.getElementById('countdown').textContent = 
          `${hours.toString().padStart(2, '0')}:${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
          
        // เปลี่ยนสีเมื่อเหลือเวลาน้อย
        const countdownElement = document.getElementById('countdown').parentElement;
        if (timeDiff < 300000) { // เหลือ 5 นาที
          countdownElement.style.background = 'linear-gradient(135deg, #dc3545 0%, #c82333 100%)';
          countdownElement.style.animation = 'pulse 1s infinite';
        } else if (timeDiff < 900000) { // เหลือ 15 นาที
          countdownElement.style.background = 'linear-gradient(135deg, #fd7e14 0%, #e55a1c 100%)';
        }
      } else {
        document.getElementById('countdown').textContent = 'หมดอายุแล้ว';
        document.getElementById('countdown').parentElement.style.background = 'linear-gradient(135deg, #dc3545 0%, #c82333 100%)';
      }
    }
    
    // อัพเดท countdown ทุกวินาที
    updateCountdown();
    setInterval(updateCountdown, 1000);
    
    // Form Validation และ Effects
    (function() {
      'use strict';
      window.addEventListener('load', function() {
        const form = document.getElementById('checkout-form');
        
        form.addEventListener('submit', function(event) {
          if (form.checkValidity() === false) {
            event.preventDefault();
            event.stopPropagation();
            
            // เพิ่มเอฟเฟกต์เมื่อ validation ไม่ผ่าน
            const invalidInputs = form.querySelectorAll(':invalid');
            invalidInputs.forEach(input => {
              input.style.animation = 'shake 0.5s';
              setTimeout(() => {
                input.style.animation = '';
              }, 500);
            });
          } else {
            // เอฟเฟกต์เมื่อ submit สำเร็จ
            const submitBtn = form.querySelector('.btn-submit');
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>กำลังดำเนินการ...';
            submitBtn.disabled = true;
          }
          form.classList.add('was-validated');
        }, false);
        
        // เพิ่มเอฟเฟกต์เมื่อ input focus
        const inputs = form.querySelectorAll('.form-control');
        inputs.forEach(input => {
          input.addEventListener('focus', function() {
            this.parentElement.style.transform = 'scale(1.02)';
            this.style.borderColor = '#667eea';
          });
          
          input.addEventListener('blur', function() {
            this.parentElement.style.transform = 'scale(1)';
            if (!this.matches(':focus')) {
              this.style.borderColor = '#e9ecef';
            }
          });
          
          // Real-time validation
          input.addEventListener('input', function() {
            if (this.checkValidity()) {
              this.style.borderColor = '#28a745';
            } else {
              this.style.borderColor = '#dc3545';
            }
          });
        });
        
        // เพิ่ม CSS สำหรับ animations
        const style = document.createElement('style');
        style.textContent = `
          @keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-5px); }
            75% { transform: translateX(5px); }
          }
          
          @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.7; }
            100% { opacity: 1; }
          }
          
          .form-control:valid {
            border-color: #28a745 !important;
          }
          
          .form-control:invalid {
            border-color: #dc3545 !important;
          }
        `;
        document.head.appendChild(style);
      }, false);
    })();
    
    // เพิ่มเอฟเฟกต์ loading เมื่อโหลดหน้า
    window.addEventListener('load', function() {
      document.body.style.opacity = '0';
      document.body.style.transform = 'translateY(20px)';
      
      setTimeout(() => {
        document.body.style.transition = 'all 0.5s ease';
        document.body.style.opacity = '1';
        document.body.style.transform = 'translateY(0)';
      }, 100);
    });
  </script>
</body>
</html>
