<div class="max-w-2xl mx-auto p-8 bg-white rounded-lg shadow-md">
  <h1 class="text-3xl font-bold mb-6">โปรไฟล์ของฉัน</h1>

  <div class="space-y-4">
    <div>
      <p class="font-semibold">ชื่อ:</p>
      <p class="text-gray-700"><%= current_user.name %></p>
    </div>
    <div>
      <p class="font-semibold">อีเมล:</p>
      <p class="text-gray-700"><%= current_user.email %></p>
    </div>
    <div>
      <p class="font-semibold">เลขบัญชีธนาคาร:</p>
      <p class="text-gray-700"><%= current_user.bank_account_number.presence || '-' %></p>
    </div>
    <div>
      <p class="font-semibold">ชื่อบัญชี:</p>
      <p class="text-gray-700"><%= current_user.bank_account_name.presence || '-' %></p>
    </div>
    <div>
      <p class="font-semibold">ธนาคาร:</p>
      <p class="text-gray-700"><%= current_user.bank_name || current_user.bank_code || '-' %></p>
    </div>
    <div>
      <p class="font-semibold">ยอดเครดิต:</p>
      <p class="text-gray-700">฿<%= number_with_precision(current_user.credit_balance, precision: 2) %></p>
    </div>
    <div class="flex justify-end">
      <%= link_to edit_profile_path, class: "inline-flex items-center space-x-2 px-4 py-2 bg-primary-600 text-white font-medium rounded hover:bg-primary-700 transition" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5h6M11 9h6M11 13h6M5 7h.01M5 11h.01M5 15h.01M3 21h18"/></svg>
        <span>แก้ไขโปรไฟล์</span>
      <% end %>
    </div>
    <div class="border-t pt-4">
      <h2 class="text-xl font-bold mb-4">การเชื่อมต่อ Facebook</h2>

      <%# --- นี่คือ Logic สำคัญ --- %>
      <% if current_user.provider == "facebook" %>
        <div class="p-4 bg-green-100 text-green-800 rounded-lg flex items-center space-x-3">
          <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
          <span>เชื่อมต่อกับบัญชี Facebook เรียบร้อยแล้ว</span>
        </div>
      <% else %>
        <p class="text-gray-600 mb-4">
          เชื่อมต่อบัญชี Facebook ของคุณเพื่อเปิดใช้งานระบบรับคอมเมนต์จากไลฟ์สด
        </p>
        <%= link_to "/auth/facebook", class: "inline-flex items-center space-x-2 px-6 py-3 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition-colors" do %>
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
          <span>เชื่อมต่อกับ Facebook</span>
        <% end %>
      <% end %>
    </div>
  </div>
</div>